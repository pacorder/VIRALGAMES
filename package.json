{
  "name": "cristoplay-juegos",
  "version": "1.0.0",
  "description": "CristoPlay • Juegos web retro, puzles y relax.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist build && cp -r *.html *.txt *.xml IMAGENES dist/ 2>/dev/null && cp -r *.html *.txt *.xml IMAGENES build/ 2>/dev/null || true",
    "start": "node server.js",
    "lint": "echo 'No lint step required'"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
